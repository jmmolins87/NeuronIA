# ClinvetIA environment variables
#
# Preferred Neon (Vercel Marketplace) variables:
# - DATABASE_URL (pooled)
# - DATABASE_URL_UNPOOLED (direct)
#
# Fallbacks (only if present in your Vercel/Neon integration):
# - POSTGRES_PRISMA_URL
# - POSTGRES_URL_NON_POOLING

APP_URL=https://clinvetia.com

DATABASE_URL=
DATABASE_URL_UNPOOLED=

POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=

ADMIN_API_KEY=

# Booking config (Paso 2)
BOOKING_TIMEZONE=Europe/Madrid
BOOKING_START_TIME=09:00
BOOKING_END_TIME=17:30
BOOKING_SLOT_MINUTES=30
HOLD_TTL_MINUTES=20

# Dev-only: allow x-debug-now header (never enable in prod)
ALLOW_TIME_OVERRIDE=false
